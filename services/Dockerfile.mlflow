# Pull the latest stable 3.x image
FROM ghcr.io/mlflow/mlflow:v2.17.2

# Install psycopg2 for Postgres backend
# Note: 3.x images are highly optimized; psycopg2-binary 2.9.9 is still standard
RUN pip install psycopg2-binary==2.9.9

# Expose port
EXPOSE 5000